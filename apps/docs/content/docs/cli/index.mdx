---
title: CLI commands
description: Primitree CLI commands for building, diffing, checking, and exporting design tokens.
---

The CLI requires Node.js 24 or newer. Add it as a development dependency:

```bash
pnpm add -D @primitree/cli
```

Binary name: `primitree`.

## Commands

| Command  | Purpose                                            |
| -------- | -------------------------------------------------- |
| `build`  | Write token JSON, Resolver, CSS, Tailwind, and TS  |
| `diff`   | Semantic changelog between two exports             |
| `check`  | Validate export or built tokens directory          |
| `init`   | Create a token repository with sample data         |
| `export` | Download variables via Figma REST API (Enterprise) |

```bash
primitree <command> --help
```

## Typical workflow

```bash
primitree init my-tokens
cd my-tokens
# replace variables.json with your export
npm install
npm run check
npm run diff
npm run build
```

<Cards>
  <Card
    title='build'
    href='/docs/cli/build'
    description='Generate the pipeline'
  />
  <Card
    title='diff'
    href='/docs/cli/diff'
    description='Review token changes'
  />
  <Card
    title='check'
    href='/docs/cli/check'
    description='Validate before merge'
  />
  <Card
    title='init'
    href='/docs/cli/init'
    description='Start a tokens repo'
  />
  <Card
    title='export'
    href='/docs/cli/export'
    description='Enterprise REST download'
  />
</Cards>
